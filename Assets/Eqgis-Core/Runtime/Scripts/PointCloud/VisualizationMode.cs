namespace Holo.PointCloud
{
    /// <summary>
    /// 渲染模式
    /// </summary>
    public enum VisualizationMode
    {
        /// <summary>
        /// 绘制所有特征点
        /// </summary>
        All,

        /// <summary>
        /// 绘制当前帧的特征点
        /// </summary>
        CurrentFrame,
    }
}